name: flutter_image_optimizer
description: Automatic image optimization, compression, and format conversion for Flutter applications
version: 0.1.0
homepage: https://github.com/Dhia-Bechattaoui/flutter_image_optimizer
repository: https://github.com/Dhia-Bechattaoui/flutter_image_optimizer
issue_tracker: https://github.com/Dhia-Bechattaoui/flutter_image_optimizer/issues

environment:
  sdk: '>=3.8.0 <4.0.0'
  flutter: ">=3.32.0"

topics:
  - flutter
  - image
  - optimization
  - compression
  - webp

funding:
  - https://github.com/sponsors/Dhia-Bechattaoui

dependencies:
  flutter:
    sdk: flutter
  image: ^4.0.0
  path_provider: ^2.0.0
  permission_handler: ^12.0.0
  ffi: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  test: ^1.0.0

flutter:
  # No Flutter-specific dependencies needed for this package
